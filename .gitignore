# =========================================================
# Frontend (FE) - 자동 생성 파일만 무시
# =========================================================

# FE의 package-lock.json (자주 변경되는 자동 생성 파일)
FE/package-lock.json

# FE의 node_modules는 이미 FE/.gitignore에서 처리됨

# =========================================================
# 참고: FE 폴더 자체는 추적하되, 자동 생성 파일만 무시
# FE 작업이 필요한 경우 명시적으로 add 해야 함
# =========================================================

